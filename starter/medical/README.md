# Starter — 智慧診所 / AI Clinic

候診看板的起始專案，對應 [`workshops/medical/03-prompts.md`](../../workshops/medical/03-prompts.md) 的 Prompt 1~5。`index.html` 與 `assets/js/app.js` 裡標了 `TODO（Prompt N）` 的地方，就是每一步 Prompt 預期會補上的內容。

## 本地預覽

用瀏覽器直接開啟 `index.html` 即可，不需要伺服器（這個專案沒有用到 `fetch()` 讀取外部檔案）。

## 結構

```
index.html
assets/
├── css/style.css
└── js/app.js   # patients 資料 + render() 待完成
```
